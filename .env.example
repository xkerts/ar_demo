# Base URL for the application
BASE_URL=https://localhost:3000

# Path to 3D models (relative to public directory)
MODELS_PATH=/models

# API endpoint (for future use when connecting to real backend)
# API_URL=https://api.example.com
